#include <bits/stdc++.h>
using namespace std;
int main(){
    int x, step;
    cin >> x;
    cin >> step;
    while(x != 0){
        vector<int> v;
        for(i = 0; i <= x; i++)
            v.push_back(0);
        int min = 2, cur = 1, count = 0;
        while(count < x){
            v[cur] = 1;
            count++;
            cur += 
        }
    }
}
